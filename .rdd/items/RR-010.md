---
id: RR-010
type: rr
parent: null
status: completed
owner: human
created: 2026-08-24
persists: permanent
spec: SPEC-016
---

# Raw Requirement: 优化 ChatView 代码，拆分组件

## 来源
用户：优化ChatView代码，提出chatview组件、状态栏组件、输入框组件、封装 useCommand hook

## 业务目标
- ChatView 单文件复杂度高（~890 行），拆分成职责单一的组件和 hook
- 提取 ChatView 组件（主容器，只管布局和协调）
- 提取 StatusBar 组件（已独立，保留）
- 提取 InputBox 组件（输入框 + 斜杠命令弹层 + @file 弹层 + chips）
- 提取 useCommand hook（斜杠命令执行逻辑、状态管理、过滤）
- 提取 ToolRow（已独立）、TurnBlock（已独立）
- 提取 MessageList（渲染 renderItems 列表 + typing indicator + 空状态）

## 范围
- web/components/ChatView.tsx：拆分，只保留布局和协调逻辑
- web/components/InputBox.tsx：新文件，输入框 + 弹层 + chips
- web/components/MessageList.tsx：新文件，renderItems 渲染
- web/hooks/useCommand.ts：新文件，斜杠命令逻辑（executeCommand、runCommand、commandMode、filtered、highlight）
- 保留 ToolRow、TurnBlock、StatusBar 在各自文件或独立文件

## 未定
- 具体文件边界和接口
- 是否需要拆分 @file 相关逻辑到单独 hook

## 验证结果（SPEC-016 行为保持契约）
- AC-001/002/003 空态/typing/非pending：既有 ChatView.test（重构后仍通过）✓
- AC-004 命令弹层键盘（Enter 执行 / ArrowDown 移动 / Esc 清空）：InputBox.test ✓
- AC-005 文件弹层 + chips（Enter 选中 / Backspace 删末尾 / 删除按钮）：InputBox.test ✓
- AC-006 ToolRow 折叠：ToolRow.test ✓
- AC-009 next build 无错：✓
- 全测试：domain 41/41 + web 74/74（重构零行为回归）
- 手动走查：待用户起 dev 走查 sub-agent 折叠 / StatusBar（AC-007/008 展示型，单测+build 已覆盖编译与渲染契约）
