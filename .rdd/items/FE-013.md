---
id: FE-013
type: feature
parent: RR-007
status: completed
owner: human
created: 2026-08-24
persists: permanent
epic: "skills/rules/MCP 两层（Global + Project）+ 合并"
---

# Feature: skills/rules/MCP 两层 + 合并加载

## 现状（Impact）
- skills：loadSkills 仅读 <workspace>/.anycode/skills/（项目级），无全局层
- rules：loadRule 仅读 <workspace>/.anycode/rules/（项目级），无全局层
- MCP：loadMcpTools 仅读全局 config.mcpServers（~/.anycode/config.yaml mcp 段），无项目层
- memory：已两层合并 ✓（无需改）
- web /settings：仅编辑全局 config（providers + mcp）✓（无需改）

## Stories（Lite 内联）
- US-A skills 两层：loadSkills 合并全局 ~/.anycode/skills/*.md + 项目 .anycode/skills/*.md
- US-B rules 两层：loadRule 合并全局 ~/.anycode/rules/*.md + 项目 .anycode/rules/*.md
- US-C MCP 两层：loadMcpTools 合并全局 config.mcp + 项目 <workspace>/.anycode/mcp 文件
- US-D web /settings 不变（仅全局）；项目级用户手动加文件

## Open Questions（全已决策 → DEC-045/046，见 SPEC-013）
- Q-1 MCP 项目文件 → <workspace>/.anycode/mcp.yaml（flat servers map）(DEC-045)
- Q-2 合并 → 项目覆盖全局（同名项目优先），不同名并集 (DEC-046)

## 实现记录
- domain：
  - `skill.ts`/`rule.ts`：loadSkills/loadRule 合并全局 `~/.anycode/{skills,rules}/` + 项目 `<workspace>/.anycode/{skills,rules}/`（同名项目覆盖全局）
  - `mcp.ts`：新增 `loadProjectMcp(workspace)` 读 `<workspace>/.anycode/mcp.yaml`（flat servers map）
  - `main.ts`：AnyAgent.initMcp 合并全局 config.mcpServers + 项目 mcp.yaml（项目覆盖同名）后 loadMcpTools
  - memory 已两层（无改）；web /settings 仅全局（无改）
- 测试：`layers.test.ts`（6）：skills/rules 两层合并 + 同名覆盖 / loadProjectMcp / 无全局层仅项目 / 无项目层仅全局
- 验证：domain tsc 0 + 37/37 + build
- deferred：status 面板 skill 数反映合并（Q-013a）、项目 mcp 热更新（Q-013b）
