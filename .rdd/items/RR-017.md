---
id: RR-017
type: rr
parent: null
status: completed
owner: human
created: 2026-08-24
persists: permanent
spec: SPEC-023
---

# Raw Requirement: maxOutputTokens 配置（简化）

## 来源
用户：增加 max output tokens 配置。后追问"需不需要"→ 调研业界主流 harness → 照业界主流 harness 保守做法简化（不探测/不取 min）。

## 决策（Human 决）
- 保留配置项（LlmProvider.maxOutputTokens optional + /settings 输入）
- 但简化：去掉 detect/resolve/内置表/min——纯用户配置 fallback（配则传 max_tokens，不配则不传，provider 默认）
- 照业界主流 harness：探测不进请求路径，不拿无人选的数 cap 每次请求（业界主流 harness 的 catalog 实现注释）

## 验证结果（SPEC-023，简化后）
- AC-001 maxOutputTokens 用户配→透传 / 未配→undefined：config.test ✓
- AC-002 callLLM payload.max_tokens（有则传，undefined 不传）：llm.test ✓
- AC-003 settings 保留 + 真 run 未配→不传 ✓
- domain tsc 0 + 82/82；web tsc 0 + 98/98；build ✓
- 注：contextWindow 仍保留探测+min（探测对 dashscope 有效且只影响进度条）；maxOutputTokens 不照搬
